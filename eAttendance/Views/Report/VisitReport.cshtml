@using System.Linq
@using eAttendance.Models
@model  eAttendance.ReportModel.EmployeeAttendanceList
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Visit Report";
}
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
<script src="~/Scripts/ExcelExport.js"></script>

<div id="divLoading" style="margin: 0px; padding: 0px; position: fixed; right: 0px;
                                      top: 0px; width: 100%; height: 100%; background-color: #666666; z-index: 30001;
                                      opacity: .8; filter: alpha(opacity=70); display: none">
    <p style="position: absolute; top: 30%; left: 45%; color: White; text-align: center;">
        <img src="~/Images/ajax-loading.gif" />
        <br />
        Loading Please Wait...........
    </p>
</div>

<script>

    function ShowLoader() {
        $("#divLoading").show();
    }
    function EndLoader() {
        $("#divLoading").hide();
    }
</script>
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-8 col-sm-8">
        <h2>@eAttendance.Service.MenuService.VisitReport</h2>
        <ol class="breadcrumb">
            <li><a href="@Url.Action("Index","Home")">घर</a></li>
            <li>@eAttendance.Service.MenuService.Report</li>
            <li class="active"><strong>@eAttendance.Service.MenuService.VisitReport</strong></li>
        </ol>
    </div>
    <div class="col-lg-4 col-sm-4">
        <div class="title-action">
           
            <a download="Visit Report.xls" class="btn btn-success btn_excel fa fa-file-excel-o" href="#" onclick="return ExcellentExport.excel(this, 'divprint', 'Daily Report');">Excel मा लैजानुहोस् </a>



            <button type="button" id="btnPrint" class="btn btn-primary"><i class="fa fa-print"></i> Print </button>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="wrapper wrapper-content animated fadeInDown no-margins padding-top-md">
            <div class="ibox float-e-margins no-margins">
                <div class="ibox-title">
                    <h5><i class="fa fa-filter"></i> @eAttendance.Service.MenuService.Filter</h5>
                    <div class="ibox-tools">
                        <a class="collapse-link">
                            <i class="fa fa-chevron-up"></i>
                        </a>
                    </div>
                </div>

                <div class="ibox-content no-border-bottom">


                    @using (Ajax.BeginForm("VisitReport", "Report", new AjaxOptions { HttpMethod = "Post", OnBegin = "ShowLoader", OnComplete = "EndLoader", UpdateTargetId = "divprint" }))
                    {
                        <div class="row">
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">@eAttendance.Service.MenuService.FromDate</label>
                                    @Html.TextBoxFor(m => m._nFromDate, "", new { @class = "form-control inputmask nepali-calendar input-sm", data_mask = "9999-99-99", @id = "_nFromDate" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">@eAttendance.Service.MenuService.ToDate</label>
                                    @Html.TextBoxFor(m => m._nToDate, "", new { @class = "form-control inputmask nepali-calendar  input-sm", data_mask = "9999-99-99", @id = "_nToDate" })
                                </div>
                            </div>






                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">कार्यालय</label>
                                    @Html.DropDownListFor(m => m.OfficeId, Utility.GetOfficeIdNameWithSelect(User), new { @class = "chosen-select form-control" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">शाखा</label>

                                    @Html.DropDownListFor(m => m.BranchId, Utility.GetBranchIdName(@User), new { @class = "chosen-select form-control" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label ">सेवा</label>
                                    @Html.DropDownListFor(m => m.ServiceId, Utility.GetServiceIdName(), new { @class = "chosen-select form-control" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">तह</label>
                                    @Html.DropDownListFor(m => m.LevelId, Utility.GetLevelIdName(), new { @class = "chosen-select form-control" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">पद</label>
                                    @Html.DropDownListFor(m => m.DesignationId, Enumerable.Empty<SelectListItem>(), new { @class = "chosen-select form-control" })
                                </div>
                            </div>
                            <div class="col-sm-3">
                                <div class="form-group">
                                    <label class="control-label">कर्मचारी र @eAttendance.Service.MenuService.EmployeeNo</label>
                                    @Html.DropDownListFor(m => m.EmployeeId, Enumerable.Empty<SelectListItem>(), new { @class = "chosen-select form-control chosen-select" })
                                </div>
                            </div>






                            <div class="col-sm-3">
                                <div class="form-group">
                                    <button type="submit" class="btn-Search btn btn-primary btn-rounded btn-outline fa fa-search"> खोज्नुहोस्</button>
                                </div>
                            </div>
                        </div>

                    }
                </div>
            </div>
            <div id="divprint" class="wrapper wrapper-content animated fadeInDown no-margins no-padding padding-top-md">
                @{
                    Html.RenderPartial("_Visit", Model);
                }
            </div>
        </div>
    </div>
</div>
@section scripts
{
    <script>

        $(function () {
            $("body").addClass("pace-done skin-1 mini-navbar");
            $(".nepali-calendar").nepaliDatePicker({
                onChange: function () {
                    CompareDate();
                    UpdateChange();
                }
            });

            $(".chosen-select").chosen({ width: '100%' });

            $.validator.setDefaults({ ignore: ":hidden:not(select)" });

            UpdateChange();

            $("#frmReport").validate({
                rules: {
                    "officeId": {
                        min: 1,
                        required: true
                    },
                    "_nFromDate": {
                        required: true
                    },
                    "_nToDate": {
                        required: true
                    }
                },
                messages:
                {
                    "officeId": {
                        min: 'कृपया विकल्प चयन गर्नुहोस्'
                    },
                    "_nFromDate": {
                        required: 'कृपया विकल्प चयन गर्नुहोस्'
                    },
                    "_nToDate": {
                        required: 'कृपया विकल्प चयन गर्नुहोस्'
                    }
                }
            });




            $('#OfficeId').on('change', function (e) {
                UpdateChange();
            });
            $('#BranchId').on('change', function (e) {
                UpdateChange();
            });
            $('#ServiceId').on('change', function (e) {
                UpdateChange();
            });
            $('#LevelId').on('change', function (e) {
                UpdateChange();
            });
            $('#DesignationId').on('change', function (e) {
                UpdateChange();
            });





            function UpdateChange() {
                debugger;
                var fromDate = $('#_nFromDate').val() == null ? "" : $('#_nFromDate').val();
                var toDate = $('#_nToDate').val() == null ? "" : $('#_nToDate').val();



                var officeId = $("#OfficeId").val();
                var branchId = $("#BranchId").val();
                var serviceId = $("#ServiceId").val();
                var levelId = $("#LevelId").val();
                var designationId = $("#DesignationId").val();








                if (fromDate == "" || toDate == "" || fromDate == "____-__-__" || toDate == "____-__-__" || officeId <= 0) {
                    return;
                }
                //int year, int month, int officeId, int branchId = 0, int serviceId = 0, int levelId = 0, int designationId = 0, bool withAll = false, bool withSelect = false
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("GetEmployeeBy_FromDate_ToDate_OfficeIdAndFilteringWithAllOrSelect", "Report")',
                    data: "{'nFromDate':'" + fromDate + "','nToDate':'" + toDate + "','officeId':'" + officeId + "','branchId':'" + branchId + "','serviceId':'" + serviceId + "','levelId':'" + levelId + "','designationId':'" + designationId + "','withAll':'" + true + "','withSelect':'" + false + "'}",
                    contentType: "application/json; charset=utf-8",
                    dataType: 'json',
                    cache: false,
                    success: function (data) {
                        debugger;
                        $("#EmployeeId").empty();
                        var $prevGroup, prevGroupName;
                        $.each(data, function () {
                            if (this.Group == null) { $("<option />").val(this.Value).text(this.Text).appendTo('#EmployeeId'); }
                            else {
                                if (prevGroupName != this.Group.Name) { $prevGroup = $('<optgroup />').prop('label', this.Group.Name).appendTo('#EmployeeId'); }
                                $("<option />").val(this.Value).text(this.Text).appendTo($prevGroup);
                                prevGroupName = this.Group.Name;
                            }
                        });
                        $("#EmployeeId").trigger('chosen:updated');
                    },
                    error: function () {
                        alert("Failed to load Item");
                    }
                });

            }


            $("#LevelId").chosen().change(function () {

                var levelId = $(this).val();
                $.getJSON("@Url.Action("GetDesignationByLevelId", "Utility")", { LevelId: levelId, withAll: true, withSelect: false },
                     function (data) {
                         $("#designationId").empty();
                         var $prevGroup, prevGroupName;
                         $.each(data, function () {
                             if (this.Group == null) { $("<option />").val(this.Value).text(this.Text).appendTo('#DesignationId'); }
                             else {
                                 if (prevGroupName != this.Group.Name) { $prevGroup = $('<optgroup />').prop('label', this.Group.Name).appendTo('#DesignationId'); }
                                 $("<option />").val(this.Value).text(this.Text).appendTo($prevGroup);
                                 prevGroupName = this.Group.Name;
                             }
                         });
                         $("#designationId").trigger('chosen:updated');
                     });
            });



            function CompareDate() {
                var fromDate = $('#_nFromDate').val() == null ? "" : $('#_nFromDate').val();
                var toDate = $('#_nToDate').val() == null ? "" : $('#_nToDate').val();
                if (fromDate != '' && toDate != '' && fromDate != '____-__-__' && toDate != '____-__-__' && fromDate != null && toDate != null) {
                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("FromDateToDateCompareRemark", "Report")',
                        data: "{'nFromDate':'" + fromDate + "','nToDate':'" + toDate + "'}",
                        contentType: "application/json; charset=utf-8",
                        dataType: 'json',
                        cache: false,
                        success: function (data) {
                            //console.log(data)
                            if (data == 2) {
                                var date = $('#_nFromDate').val();
                                $('#_nToDate').val(date);
                            }
                        },
                        error: function () {
                            alert("Failed to load Item");
                        }
                    });
                }
            }

            $('#btnPrint').on('click', function () {
                printReport('Visit Report');
                
            });


        });



    </script>
}




